box: debian
build:
  steps:
    - arjen/hugo-build:
        theme: minimo
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: mattkopecki.com
        basedir: public